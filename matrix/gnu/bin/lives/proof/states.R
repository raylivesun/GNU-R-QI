#!/usr/bin/r

# but deference vs themes books mean like idiot class pounding just except
y = c(a1=11, b1=12, c1=13, a2=21, b2=22, c2=24, a3=31, b3=32, c3=33)

# @everyone the good all hope this social text or export text
Ib <- function(x, y, z) {
      if (x == is.array(x)) {
          print(x*x^4)
      } else {
        return(x)
      }
  if (y == is.array(x)) {
    print(y*y^8)
  } else {
    return(y)
  }
  if (z == is.array(x)) {
    print(z*z^16)
  } else {
    return(z)
  }
}

pB <- function(rest, x, y, z, hip) {
   x <- sin(2)
   y <- exp(2)
   z <- x + y
   hip <- sqrt(4)
   
   if (x == sin(2)) {
      print(hip^x)
   } else {
     return(x)
   }
   if (y == exp(2)) {
     print(hip^y)
   } else {
     return(y)
   }
   if (z == x + y) {
     print(hip^z)
   } else {
     return(z)
   }
}

bb <- data.frame(x = c(20,35,45,55,70), n = rep(50,5),
                       y = c(6,17,26,37,44))

g2 <- data.frame(x = c(20,35,45,55,70), n = rep(50,5),
                 y = c(6,17,26,37,44))
g2 * 2
g2 * 3
g2 * 4
g2 * 5

