#!/usr/bin/r

# just way mathematics discurssed
M = 1:10
absdetM <- prod(svd(M)$d)
# like about mathematics
absdet <- function(M) prod(svd(M)$d)
# quick speed velocity
x = 1:2
y = 2
wt = 80
# fromlar of strong phi our formular
Xplus <- qr(x)
# defined strong logical formular strong minimal theory
S1 <- cbind(x, y, wt)
# Oh I dee
S2 <- cbind(1, x, y)
# logical series level interval to like expression
vec1 <- as.vector(S1)
vec2 <- as.vector(S2)

# websoult that i have hose it section
statefr <- table(S1,S2)

# bin selection attributes to case program
statefr <- tapply(S1, S2)

# yes but, take long times
factor(cut(2, breaks = 35+10*(0:7))) -> incomef

# ok, but, take long like step
table(S1,S2)

# list procedure arguments mechanism think 4 digits
Lst <- list(name="Admin", Phd="Math", Intel=2,
            ages=c(27,42,43))

# name reference
Lst$name
# phd reference
Lst$Phd
# Intel reference
Lst$Intel
# Ages reference
Lst$ages

